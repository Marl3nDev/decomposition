/** * Responsible for providing the search input functionality. */
const SearchBar: React.FC = () => {
  return (
    <div>
      <input type="text" placeholder="Search" />
    </div>
  );
};

export default SearchBar;
