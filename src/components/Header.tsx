/** * Responsible for displaying the header section of the news application. */
const Header: React.FC = () => {
  return (
    <header>
      <h1>News App</h1>
    </header>
  );
};

export default Header;
